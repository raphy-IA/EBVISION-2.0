/* ========================================
   CORRECTIONS AGGRESSIVES POUR LA SIDEBAR
   ======================================== */

/* Forcer la largeur de la sidebar */
.sidebar-container {
    width: 250px !important;
    max-width: 250px !important;
    min-width: 250px !important;
}

/* Corriger le contenu principal - ÉLIMINER LE DÉCALAGE */
.main-content {
    margin-left: 250px !important;
    width: calc(100vw - 250px) !important;
    max-width: calc(100vw - 250px) !important;
    padding-left: 0 !important;
    padding-right: 0 !important;
}

/* Corriger les conteneurs qui pourraient avoir des marges */
.container-fluid {
    padding-left: 0 !important;
    padding-right: 0 !important;
    margin-left: 0 !important;
    margin-right: 0 !important;
}

/* Corriger les pages qui utilisent l'ancienne structure */
.col-md-3.col-lg-2.px-0.sidebar-container {
    display: none !important;
}

/* S'assurer que la sidebar est bien positionnée */
#sidebarContainer {
    width: 250px !important;
    max-width: 250px !important;
    min-width: 250px !important;
}

/* Corriger les marges des pages */
body {
    margin: 0 !important;
    padding: 0 !important;
    overflow-x: hidden !important;
}

/* Corriger les pages qui ont des styles inline */
.sidebar {
    width: 250px !important;
    max-width: 250px !important;
    min-width: 250px !important;
}

/* Responsive fixes */
@media (max-width: 768px) {
    .sidebar-container {
        width: 100% !important;
        max-width: 100% !important;
        min-width: 100% !important;
    }
    
    .main-content {
        margin-left: 0 !important;
        width: 100vw !important;
        max-width: 100vw !important;
    }
}

/* Corriger les pages qui ont des structures différentes */
.row {
    margin-left: 0 !important;
    margin-right: 0 !important;
}

/* S'assurer que le contenu ne déborde pas */
.container-fluid {
    max-width: none !important;
}

/* Corriger les pages avec des layouts Bootstrap */
.col-md-9, .col-lg-10 {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* CORRECTIONS SPÉCIFIQUES POUR ÉLIMINER LE DÉCALAGE */
/* Forcer la position de la sidebar */
.sidebar-container {
    position: fixed !important;
    left: 0 !important;
    top: 0 !important;
    z-index: 1000 !important;
}

/* Forcer la position du contenu principal */
.main-content {
    position: relative !important;
    left: 250px !important;
}

/* Corriger les pages avec des structures Bootstrap */
.container {
    max-width: none !important;
    width: 100% !important;
}

/* Éliminer tous les espaces inutiles */
* {
    box-sizing: border-box !important;
}

/* Corriger les pages avec des headers */
.page-header {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les tableaux et listes */
.table-responsive {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les cards et autres éléments */
.card {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les formulaires */
.form-group {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les boutons et actions */
.btn-group {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les modales */
.modal {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les tooltips */
.tooltip {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les popovers */
.popover {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les dropdowns */
.dropdown-menu {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les navs */
.nav {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les breadcrumbs */
.breadcrumb {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les paginations */
.pagination {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les alerts */
.alert {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les badges */
.badge {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les progress bars */
.progress {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les list groups */
.list-group {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les panels */
.panel {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les wells */
.well {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les jumbotrons */
.jumbotron {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les carousels */
.carousel {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les tabs */
.tab-content {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les accordions */
.accordion {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les tooltips */
.tooltip-inner {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les popover contents */
.popover-content {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les dropdown items */
.dropdown-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les nav items */
.nav-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les breadcrumb items */
.breadcrumb-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les pagination items */
.page-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les alert items */
.alert-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les badge items */
.badge-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les progress items */
.progress-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les list group items */
.list-group-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les panel items */
.panel-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les well items */
.well-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les jumbotron items */
.jumbotron-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les carousel items */
.carousel-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les tab items */
.tab-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
}

/* Corriger les accordion items */
.accordion-item {
    margin-left: 0 !important;
    padding-left: 0 !important;
} 